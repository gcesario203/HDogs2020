Gabriel Fernando da Silva Cesario
Amanda Marques Audizio
Vinius Gabriel da Silva David
Gabriel Palheiro dos Santos
Pedro Trinca
Marcos Bastos Garcia

OBS *1 = Todos CRUD's completos, e foi utilizado do gulp para auxilio de refatoramento do código, porém, os arquivos originais estão no coisas/dev/... para edição caso necessario.

OBS *2 = Caso alguém vá editar algo em relação ao front, seguir comandos:
 - Caso não ter node.js instalado, por favor instalar na máquina.
 
 - No terminal, rodar o comando npm i ou npm install para instalar as dependências do gulp.
 - Depois de realizar as alterações, seja no CSS, HTML, ou javascript, rodar o comando gulp no terminal para que as alterações sejam realizadas no projeto

OBS *3 = Para iniciar o projeto siga as instruções:
 - Caso não tiver o python instalado:
	- baixar e instalar o python
	- no terminal(vscode ou cmg), usar o comando python get-pip.py
	- criar uma venv com o comando(caso windows): python -m venv *nomedavenv
 - Caso venv ja instalada ou usuário de Linux:
	- ativar a venv:
		- caso windows: ir para o diretório da venv e rodar o comando: venv\Scripts\activate.
		- caso linux: ir para o diretorio da venv e usar o comando: venv/bin/activate.					
	- com a venv ativa: usar o comando pip install -r requirements.txt para instalar as dependencias necessarias do Django.

	- Com tudo instalado: usar o comando python manage.py runser(caso windows 7, para qualquer comando citado que tenho python, trocar para py) e clicar com o control segurado no link: localhost:8000 para ser direcionado a tela de login do projeto.

	- Por fim,manuseie o projeto a seu gosto e vontade ;)
